services:
  voice-terminal:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
      - ./data/audio:/data/audio
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
